spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flowable?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&allowMultiQueries=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
#    show-sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate: info
    org:
      hibernate:
        orm:
          jdbc:
            bind: trace

management:
  endpoints:
    web:
      exposure:
        include: "*"

